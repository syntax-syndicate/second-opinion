# Second Opinion MCP Server Requirements

# MCP Framework
mcp

# OpenAI API (for OpenAI, Grok, and DeepSeek - they all use OpenAI-compatible APIs)
openai

# Gemini API (supports both new and legacy SDKs)
google-genai
google-generativeai

# Anthropic Claude API
anthropic

# HTTP requests for HuggingFace Inference API
requests

# Standard library imports (these should be available by default)
# asyncio, json, os, sys, logging, collections, typing
